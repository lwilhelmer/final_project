function setup() {
  let canvas = createCanvas(900, 500);
  canvas.parent("map-container");
}

function draw() {
  background(220);
  textSize(32);
  fill(0);
  text("This can become your custom illustrated map", 20, 50);
}
